#!/usr/bin/env node
// subagent-orchestrator entrypoint.
// v0.1 scaffold (M0). See ../PROJECT_PLAN.md milestones for what comes next.
// TODO(M1): wire commander; subcommands `tasks list`, `dispatch`, `stats`.

console.log("subagent-orchestrator v0.0.0 — scaffold only. See PROJECT_PLAN.md.");
process.exit(0);
