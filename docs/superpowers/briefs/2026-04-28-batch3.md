# Progress Brief — 2026-04-28 batch 3

> Snapshot at the third /loop checkpoint, written before queueing the next batch. Supersedes the morning brief on the cumulative numbers; the morning brief still stands on lessons learned.

## Live state

- https://managedsubagents.com — worker `c1a098cf-387e-4143-8a5e-b0aad39e4e6c`
- All four SEO/UX additions verified live: OG + Twitter cards (meta), `robots.txt` (200), `sitemap.xml` (200), JSON-LD `SoftwareApplication` (in HTML), `Install` section (in JS bundle, rendered by React on hydration)
- Bundle: `index.html` 2.58kB · CSS 9.11kB (gzip 2.41kB) · JS 151.04kB (gzip 48.74kB)

## Cumulative ledger (today)

`dispatch_log summary --bucket day` for 2026-04-28:

```
bucket           total  ok  fail  human  inflight  cost     rate
2026-04-28       26     19  6     0      1         $12.74   76%
```

Across the day so far: **15 PRs merged**, **$12.74 spent** (~$2.72 of which was waste from the now-fixed `READ_ONLY_RE` heuristic bug).

## What changed since the morning brief

| PR  | Cost     | What                                                           |
| --- | -------- | -------------------------------------------------------------- |
| #76 | —        | Brief 1 + initial 5-task queue                                 |
| #77 | —        | Correction: dropped misdiagnosed `tasks-remove-bug`, queued real heuristic fix |
| #78 | manual   | **Fix `READ_ONLY_RE` heuristic** — `EDIT_INTENT_RE` checked first, regression test, 311/311 vitest green |
| #79 | —        | Queue prune                                                    |
| #80 | $0.37    | OG + Twitter card meta                                         |
| #81 | $0.33    | `robots.txt` + `sitemap.xml`                                   |
| #82 | $0.28    | SoftwareApplication JSON-LD                                    |
| #83 | $0.67    | In-page `Install` section (3 brutalist-terminal steps)         |
| #84 | —        | Deploy record (worker `c1a098cf`) + queue clear                |

## Open issues (carrying forward)

1. **Second-order reporting bug** (HIGH) — `dispatchLocal` reports `ready-for-merge` even when plan-mode produced no edits. Reproducible from `dispatch_log` rows #20 ($1.12) and #21 ($0.51). **Queued in this batch as `fix-readyformerge-when-no-edits`.**
2. **No `dispatch update` CLI** — stuck `dispatched` row #19 still polluting summary stats (76% success rate would be ~80% without it). Low priority; deferred again.
3. **No Cloudflare MCP** — same as morning brief; future cf-touching tasks use `wrangler` directly.
4. **OG image referenced but doesn't exist** — `<meta property="og:image" content="/og-image.png">` exists in HTML but `curl /og-image.png` returns 404. Unfurlers will see nothing. **Queued as `webapp-og-image`.**
5. **Favicon is an inline data-URI `<` glyph only** — iOS home-screen icon broken. **Queued as `webapp-favicon`.**

## Next batch (4 tasks)

Discriminator: every task has a curl-or-test-verifiable outcome. Same one-at-a-time dispatch pattern; single `wrangler deploy` at the end.

1. **`fix-readyformerge-when-no-edits`** (local) — `dispatchLocal` returns `failed` with reason "plan-mode produced no edits" when it ran in plan mode AND the SDK message stream contained no `Write`/`Edit` tool uses. Regression test pins the exact failure mode rows #20/#21 hit.
2. **`webapp-og-image`** (web) — generate `webapp/public/og-image.png` (1200×630, brutalist phosphor-green ASCII art consistent with Hero), so unfurlers actually have an image. Use Sharp via Node script.
3. **`webapp-favicon`** (web) — replace inline data-URI favicon in `index.html` with proper PNG set: `favicon-16.png`, `favicon-32.png`, `apple-touch-icon.png` (180×180), all served from `webapp/public/`. Update `<link>` tags accordingly.
4. **`webapp-changelog-section`** (web) — new `Changelog` in-page section between `Install` and `SelfRef`; reads from `webapp/src/data/changelog.ts` (a frozen list of recent merged PRs, hand-checked-in for now). Six entries: PRs #78, #80–#84.

After this batch lands, the queue stays empty until the next user-given direction. The /loop should not keep generating batches against an idle queue.
